# Tomato Kiosk · 포인트 화면 뒤로가기 분기 보정 v4.3.4

## 적용 방법

이 ZIP은 기존 star0301/kioskUI 저장소에 덮어쓰는 소스 업데이트입니다.
기존 저장소의 assets 폴더는 그대로 유지해 주세요.

1. ZIP을 풉니다.
2. index.html이 있는 기존 Git 저장소 루트에 아래 파일을 덮어씁니다.
   - index.html
   - styles.css
   - app.js
   - modals.js (새 파일)
3. 로컬에서 index.html을 열거나 정적 서버로 확인합니다.
4. 변경 파일을 커밋하고 main 브랜치로 푸시합니다.

권장 명령:

    git add index.html styles.css app.js modals.js DEPLOY-NOTES.md
    git commit -m "feat: align kiosk modals with Figma flow"
    git push origin main

GitHub Pages가 main 브랜치에서 배포되도록 이미 설정돼 있다면 푸시 후 자동으로
다시 배포됩니다. 반영 후에는 Ctrl + F5로 캐시를 무시하고 새로고침해 주세요.

## 주요 변경

- 이미 로그인된 회원이 결제수단에서 포인트 화면으로 진입한 경우 `뒤로` 선택 시 장바구니 화면으로 복귀
- 결제 과정에서 처음 로그인한 경우에는 기존처럼 포인트 화면에서 로그인 화면으로 복귀 가능
- APP·휴대폰·포인트 사용 불가 화면의 뒤로가기 규칙을 공통 함수로 통일
- 정적 리소스 캐시 버전을 `v4.3.4`로 갱신
- 포인트 적립·사용 내역의 모든 표 셀을 중앙 정렬
- 헤더 `포인트 조회`에서 로그인 성공 시 H1 포인트 적립·사용 내역으로 이동
- 결제 과정의 로그인 성공은 기존처럼 APP·휴대폰 포인트 사용 화면으로 이동
- 로그인 화면의 뒤로가기를 진입 목적에 맞게 분기
- 정적 리소스 캐시 버전을 `v4.3.3`으로 갱신
- `사용 가능 전액`과 포인트 값을 항상 두 줄로 분리해 표시
- 포인트 비밀번호 인증 성공 후 결제 직행 대신 포인트 적용 화면으로 복귀
- 인증 완료 상태에서 `적용하고 결제 계속`을 눌러야 실제 결제 시작
- 포인트 선택값 변경 시 기존 비밀번호 인증 상태 초기화
- 정적 리소스 캐시 버전을 `v4.3.2`로 갱신
- 회원 로그인 화면의 `010`, 하이픈, 4자리 번호 그룹을 동일한 수평 기준선으로 정렬
- 입력 전후 숫자 슬롯 폭을 고정하여 번호 사이 간격과 전체 중앙 정렬 유지
- 빈 슬롯의 밑줄을 독립 요소로 처리하여 숫자 입력 시 레이아웃 이동 방지
- 정적 리소스 캐시 버전을 `v4.3.1`로 갱신
- 봉투 선택 즉시 장바구니에 담고 선택창 닫기
- 회원 혜택 화면의 뒤로가기 및 회원 로그인 사용 OFF 시 결제 직행
- 상품직접선택 사용 OFF 시 장바구니 전체 폭 확장 및 봉투 버튼 이동
- 휴대폰 회원 포인트 화면 간소화 및 최종 결제금액 강조
- 고객명을 제목 바로 아래 중앙 배치하고 보유 포인트·결제금액을 한 줄씩 표시
- 포인트 사용 결과와 최종 결제금액을 서로 분리된 카드로 구성
- 포인트 입력 문구와 현금 반환 금액 표기 보정
- 휴대폰 회원가입 중복 검사 및 브라우저 저장 회원 재검사
- 결제 자동 전환 대기시간 2초 연장
- 상품 추가 시 카탈로그 재렌더링 제거 및 프리셋 탭 자동 스크롤
- 04-INP 포인트 적용과 04-PHN 사용 가능 전액 선택 후 04-PIN 진입
- 결제 비밀번호 1~4회 오류 시 재입력, 5회 오류 시 E-PIN-01 안내
- E-PIN-01 닫기 시 포인트 적용값 해제 후 회원 포인트 화면 복귀
- 헤더의 포인트 조회·내역 조회 버튼을 토마토 Primary 색상으로 통일
- 휴대폰 번호 입력이 완료된 슬롯은 밑줄을 숨기고 숫자만 표시

## 시연용 결제 비밀번호

- 기본 APP·휴대폰 테스트 회원: `1234`
- 신규 휴대폰 회원: 가입 과정에서 설정한 숫자 4자리

브라우저 저장소를 사용하는 시연용 구현이므로 실제 운영 환경에서는 결제 비밀번호를
브라우저에 평문으로 저장하지 말고 서버에서 안전하게 검증해야 합니다.
